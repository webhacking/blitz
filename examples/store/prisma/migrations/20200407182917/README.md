# Migration `20200407182917`

This migration has been generated by Brandon Bayer at 4/7/2020, 6:29:17 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."Store" (
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "name" TEXT   
) 

CREATE TABLE "quaint"."Membership" (
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "name" TEXT   ,
    "roles" TEXT NOT NULL DEFAULT '' ,
    "storeId" INTEGER NOT NULL  ,
    "userId" INTEGER NOT NULL  ,FOREIGN KEY ("storeId") REFERENCES "Store"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
) 

CREATE TABLE "quaint"."User" (
    "email" TEXT NOT NULL  ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL  
) 

PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Product" (
    "description" TEXT   ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "name" TEXT   ,
    "price" INTEGER   ,
    "storeId" INTEGER NOT NULL  ,FOREIGN KEY ("storeId") REFERENCES "Store"("id") ON DELETE CASCADE ON UPDATE CASCADE
) 

INSERT INTO "quaint"."new_Product" ("description", "id", "name", "price") SELECT "description", "id", "name", "price" FROM "quaint"."Product"

DROP TABLE "quaint"."Product";

ALTER TABLE "quaint"."new_Product" RENAME TO "Product";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200313161945..20200407182917
--- datamodel.dml
+++ datamodel.dml
@@ -5,14 +5,14 @@
 // Set the `provider` field to match your DB type: "postgresql", "mysql" or "sqlite".
 // The `url` field must contain the connection string to your DB.
 // Learn more about connection strings for your DB: https://pris.ly/d/connection-strings
 datasource db {
-  provider = "sqlite" // other options are: "mysql" and "sqlite"
-  url = "***"
+  provider = "sqlite"
+    url      = "file:../tmp/db.sqlite"
 }
 // Other examples for connection strings are:
-// SQLite: url = "***"
-// MySQL:  url = "***"
+// SQLite: url = "sqlite:./dev.db"
+// MySQL:  url = "mysql://johndoe:johndoe@localhost:3306/mydb"
 // You can also use environment variables to specify the connection string: https://pris.ly/d/prisma-schema#using-environment-variables
 // By adding the `generator` block, you specify that you want to generate Prisma's DB client.
 // The client is generated by runnning the `prisma generate` command and will be located in `node_modules/@prisma` and can be imported in your code as:
@@ -26,17 +26,38 @@
 // 2. Run `prisma2 introspect` to get your data model into the schema (this will override this file and delete all comments!)
 // 3. Run `prisma2 generate` to generate Prisma Client JS
 // 4. Start using Prisma Client JS in your application
+model Store {
+  id    Int     @id @default(autoincrement())
+    name      String?
+    memberships Membership[]
+    products Product[]
+}
+
+model Membership {
+  id    Int     @id @default(autoincrement())
+    store     Store @relation(fields: [storeId], references: [id])
+    storeId   Int
+    user     User @relation(fields: [userId], references: [id])
+    userId    Int
+    name      String?
+    roles    String @default("")
+}
+
+model User {
+  id    Int     @id @default(autoincrement())
+    name      String
+    email  String
+    memberships Membership[]
+}
+
 model Product {
   id    Int     @id @default(autoincrement())
-  name      String?
-  description String?
-  price    Int?
+    store     Store @relation(fields: [storeId], references: [id])
+    storeId  Int
+    name      String?
+    description String?
+    price    Int?
 }
-//model User {
-//  id    Int     @id @default(autoincrement())
-//  email: String
-//  roles: String @default('')
-//}
```


