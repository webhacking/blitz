# Migration `20200407183550`

This migration has been generated by Brandon Bayer at 4/7/2020, 6:35:50 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_Product" (
    "description" TEXT   ,
    "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL  ,
    "price" INTEGER   ,
    "storeId" INTEGER   ,FOREIGN KEY ("storeId") REFERENCES "Store"("id") ON DELETE SET NULL ON UPDATE CASCADE
) 

INSERT INTO "quaint"."new_Product" ("description", "id", "name", "price", "storeId") SELECT "description", "id", "name", "price", "storeId" FROM "quaint"."Product"

DROP TABLE "quaint"."Product";

ALTER TABLE "quaint"."new_Product" RENAME TO "Product";

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200407183010..20200407183550
--- datamodel.dml
+++ datamodel.dml
@@ -6,13 +6,13 @@
 // The `url` field must contain the connection string to your DB.
 // Learn more about connection strings for your DB: https://pris.ly/d/connection-strings
 datasource db {
   provider = "sqlite"
-    url = "***"
+    url      = "file:../tmp/db.sqlite"
 }
 // Other examples for connection strings are:
-// SQLite: url = "***"
-// MySQL:  url = "***"
+// SQLite: url = "sqlite:./dev.db"
+// MySQL:  url = "mysql://johndoe:johndoe@localhost:3306/mydb"
 // You can also use environment variables to specify the connection string: https://pris.ly/d/prisma-schema#using-environment-variables
 // By adding the `generator` block, you specify that you want to generate Prisma's DB client.
 // The client is generated by runnning the `prisma generate` command and will be located in `node_modules/@prisma` and can be imported in your code as:
@@ -54,9 +54,9 @@
 model Product {
   id    Int     @id @default(autoincrement())
     store     Store? @relation(fields: [storeId], references: [id])
     storeId  Int?
-    name      String?
+    name      String
     description String?
     price    Int?
 }
```


